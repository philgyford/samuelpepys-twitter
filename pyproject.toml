[project]
name = "samuelpepys-twitter"
dependencies = [
  "APScheduler",
  "configparser",
  "Mastodon.py",
  "pytz",
  "redis",
  "tweepy",
  "atproto>=0.0.54",
]
requires-python = "~=3.10.0"
version = "1.0"

[tool.uv.pip]
generate-hashes = true
python-version = "3.10"
